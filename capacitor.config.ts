import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.dungeonsweeper',
  appName: 'Dungeonsweeper',
  webDir: 'dist'
};

export default config;
